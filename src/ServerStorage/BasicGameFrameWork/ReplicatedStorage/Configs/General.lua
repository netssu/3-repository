local General = {
	GroupId = game.CreatorType == Enum.CreatorType.Group and game.CreatorId or 16829509, -- thur's studios
	StoreVersion = "v1" -- changing this value, will create a whole new datastore for entire game
}

return General